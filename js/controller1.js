/**
 * Controls the Blog
 */
app.controller('BlogCtrl', ['$scope', function ($scope) {
    var variable = "Blog Controller reporting for duty.";

    function method(){
        console.log(text);
    }

    method();
}]);